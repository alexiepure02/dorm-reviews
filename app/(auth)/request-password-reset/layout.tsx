export const metadata = {
  title: "Resetare Parolă - Căminul Tău",
};

export default function RequestPasswordResetLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
